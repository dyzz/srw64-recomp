# SRW64 阿里云图像高清化首轮实测

2026-09-08。已完成 6 份源图、4 个图像编辑模型、42 张输出的实测，并将一张受保护的高清地图绑定到真实 RT64/Metal 显示列表快照。公开价估算合计 **¥17.64**，没有读取实际账单。

**本轮看图后的方向：头像以 Qwen 3.0 候选 2 为当前首选；机械单位图标不处理，森林等战术地图 tile 暂缓，过场地图与 UI 边框的可见提升不明显，暂缓继续生成。** 文字继续用轮廓字体；窗口边框如需高清化，可另行验证规则绘制的收益。此选择限于当前样本，不代表完整游戏高清包验收或模型总体排名。

最新进展：已另建[原生内容配置](native-content-foundation.md)，使用较小的头像输出和本地 Lanczos 4 倍地图。下文保留首轮模型对照的历史范围与结果。

## 人工看图决定（2026-09-08）

- **人物头像**：用户认为 `qwen-image-3.0` 的候选 2 效果最好，将这张已保存的图片作为当前头像首选与后续对照基准。尚未在游戏内安装或推广到其他人物。
- **机械单位图标**：不需要处理，移出后续高清化范围。
- **森林等地图 tile**：目前不好处理，暂缓。
- **过场地图**：用户认为对比不明显，撤回先推进地图的建议，保留本轮候选与捕获帧回放。
- **UI 边框**：用户认为效果也不显著，暂缓继续生成；规则绘制／九宫格重建的收益尚未验证。

首选文件：[头像 Qwen 3.0 候选 2](../assets/hd-ai/2026-09-08/runs/portrait--qwen-image-3.0--2/output.png)。原始输出 SHA-256：`45ac1a1c6719c2c117853f7f8b6844f0117809b55f2b9e2d31d7dfb24e13406f`。网页和后续工作使用 [review_decisions.json](../tools/hd_ai/review_decisions.json) 记录范围与选择；保留全部 42 张历史结果。

## 可以直接查看的结果

- [交互浏览页：六类素材、全部 42 张候选、滑杆与并排对比](../assets/hd-ai/2026-09-08/gallery/index.html)

- [原画／地图高清＋既有轮廓字体对照](../assets/hd-ai/2026-09-08/runtime-comparison.png)
- [欧洲地图：首个候选对照](../assets/hd-ai/2026-09-08/review/map-europe--comparison-1.png)
- [欧洲地图：第二个候选对照](../assets/hd-ai/2026-09-08/review/map-europe--comparison-2.png)
- [头像对照](../assets/hd-ai/2026-09-08/review/portrait--comparison-1.png)
- [UI 边框对照](../assets/hd-ai/2026-09-08/review/ui-border--comparison-1.png)
- [机械图标对照](../assets/hd-ai/2026-09-08/review/mech-icon--comparison-1.png)
- [森林图集裁块对照](../assets/hd-ai/2026-09-08/review/terrain-forest--comparison-1.png)
- [完整输入清单与提示词](../assets/hd-ai/2026-09-08/samples.json)
- [输出哈希、数量、成本及指标](../assets/hd-ai/2026-09-08/review/qa.json)

`review/` 内还保留各类样本的第二候选图；`runs/` 内保留全部原始服务输出和逐次请求记录。对照图中的灰底用于查看透明区域。透明通道由本地源蒙版合成，不是模型直接生成的 Alpha。

## 模型结果与费用

| 模型 | 成功输出 | 公开价估算 | 本轮请求耗时中位数 | 本轮观察 |
| --- | ---: | ---: | ---: | --- |
| `qwen-image-3.0` | 12 | ¥2.40 | 41.98 秒 | 头像候选 2 经用户看图选为本轮首选；地图视觉收益不明显；机械小图容易重构成新造型 |
| `qwen-image-3.0-pro` | 12 | ¥6.24 | 44.67 秒 | 欧洲地图较稳，选第一个候选做了受保护合成与回放；头像仍需修订 |
| `wan2.7-image-pro` | 12 | ¥6.00 | 10.36 秒 | 本轮返回最快；地图和头像值得保留为候选；机械／森林倾向保留大像素块，新增有效细节有限 |
| `qwen-image-2.0-pro-2026-06-22` | 6 | ¥3.00 | 35.08 秒 | 固定版本便于追踪，但本轮地图山脉和头像重绘幅度较大；小机械图明显改了构图 |

耗时包含本机网络、推理与下载，是这 42 次请求的观察值，不是服务性能承诺。每次 1 张输入、1 张输出，未自动重试 POST；所有请求成功。成本按[此前核实的北京公开价](hd-ai-exploration.md)累计，未扣优惠／免费额度，不代表账单已确认。

普通超分与生成式超分共 **12 个计划输出未执行**：`GetOssStsToken` 成功返回临时凭证，但试传第一张源图到 VIAPI 临时 OSS 时返回 `403 AccessDenied`。该结果只能证明本次上传链路未通，不能推断超分服务已开通或未开通；两个超分 API 均未提交推理。没有更改 RAM 策略、创建 Bucket 或购买服务。临时凭证未写入磁盘。百炼临时上传 URL 与模型绑定，也不能直接作为另一套服务的通用输入地址。[VIAPI 文件处理](https://help.aliyun.com/zh/viapi/getting-started/the-file-url-processing)、[百炼临时 URL 限制](https://help.aliyun.com/zh/model-studio/get-temporary-file-url)

## 输入来源与适用边界

| 样本 | 来源 | 尺寸／处理 | 验证范围 |
| --- | --- | --- | --- |
| 欧洲地图 | 资源 5604 的模型显示列表、CI4 图块及独立 RGBA16 调色板 | 70 个矩形面；按顶点还原图集后取完整覆盖的 512×512 区域，翻转为北朝上 | 完整资源与捕获 RDRAM 字节相同；63 个捕获纹理的像素与调色板均逐字节匹配 |
| 亚洲地图图层 | 资源 5605，同类模型图块结构 | 还原图集的有效矩形，再裁出 640×320 区域 | 静态解码与视觉检查；未做该场景实时／回放绑定 |
| 头像 | 资源 29，96×96 CI8 | 使用真实捕获的 TLUT；灰底 RGB 推理＋保留源 Alpha | 资源字节与捕获匹配，调色板来自实际采样 |
| 装饰边框 | 资源 1296 的图条 | 裁取 `[320,0,400,16]`，在 96×96 透明画布上定位 | 使用真实捕获的 TLUT；只是一段边框，未验证九宫格全套 |
| 机械图标 | 资源 688＋共享队伍调色板 1010 | 原始 16×16，确定性最近邻预放大 | 颜色搭配做了视觉检查；实际单位映射和动画未验证 |
| 森林样片 | 图集资源 6228＋调色板 6237 | 图集裁块 `[272,16,304,48]`，32×32 | 不是完整场景；真实图块切分和地图邻接关系未核准 |

原计划的普通场景背景在这轮被森林图集裁块替代：场景背景资源虽然能解压，但其准确调色板配对尚未确认，不把猜测颜色送入模型作为正式对照。

低分辨率输入先做最近邻预放大，以满足编辑模型输入尺寸；这不会增加原始信息。输出为 2048×2048，亚洲样片为 2048×1024。最终比较也缩回相同源尺寸／4 倍尺寸，避免把文件大等同于有效高清。

亚洲样片存在一项明确的试验限制：原图水域部分透明，送入模型时合成了灰底，但沿用的地图提示词仍要求深蓝海面。此输入与提示约束不够一致，个别输出尝试补画蓝色水域。其结果仅用于暴露透明图层处理的问题，**不纳入跨模型保真排序，不作为可发布底图**。下一轮应先明确水域独立层与背景色，并修订专用提示词。

机械图标提示词也描述了对低清图像的机体结构解释。出现全身重构说明“16 像素输入＋该提示”不够约束；本轮不能把失真全部归因于模型本身。用户看图后已明确机械单位图标不需要处理，因此不继续推进这条路线。

## 地图的实际接入验证

1. 选用 `qwen-image-3.0-pro` 的欧洲地图第一个候选。
2. 从原图色板生成水域蒙版，海面和距海岸约 3 个源像素的区域保留确定性双线性放大。只在陆地内部混入 AI 结果。受保护区域相对该基线的 RGB 差异为 **0 像素**；源 Alpha 单独恢复。
3. 按原图集坐标和翻转变换切回 256×256 图块，绑定 **58 个实际出现且位于所选裁块内的 RT64 哈希**。其他图块继续使用原资源。
4. 使用原始 RDRAM、OSTask 和 RT64/Metal，在 960×720 输出中验证到第 60 次呈现；GPU 完成后读取画面。最终图块包为 [hd-pack-stall](../assets/hd-ai/2026-09-08/map-probe-final/hd-pack-stall/rt64.json)。
5. 另生成地图＋既有 22 张轮廓字形的组合包，展示文字和背景两条路径可以同时生效。该字体来自既有 [native-font-probe.md](native-font-probe.md) 实验；本轮没有让 AI 生成文字，也没有把未经审核的 AI 头像装入演示。

证据：[资源与纹理绑定](../assets/hd-ai/2026-09-08/map-capture-binding.json)、[地图保护与切块记录](../assets/hd-ai/2026-09-08/map-probe-final/build.json)、[地图回放](../assets/hd-ai/2026-09-08/replay-hd-final/report.json)、[地图与字体组合回放](../assets/hd-ai/2026-09-08/replay-map-and-font-final/report.json)。

这个快照中增加了地表纹理，但用户看图认为整体提升不明显。为保持地理轮廓，海岸仍保留原图有限采样带来的阶梯感；接入成功不能单独说明视觉收益足够。地图保留观察。

原图解码成 PNG 再替换的控制实验也没有达到整帧像素完全一致：相对同一宿主的新无包回放，691,200 个像素中 32,561 个不同，多数最大通道误差为 1；仅 100 个像素最大通道误差大于 2，RGB 平均绝对误差为 0.01922／255。不能把这个控制实验标成零差异。源纹理／调色板字节匹配与最终渲染逐像素相同是两种不同证据。[渲染对照记录](../assets/hd-ai/2026-09-08/render-check.json)

初次高清包使用 `preload` 时触发 RT64 `ReplacementMap::addLoadedTexture` 的重复条目断言。为完成本次隔离回放，包改用官方支持的 `stall` 加载，成功取得输出；没有修改渲染器源码，也没有把断言的根因视为已修复。生产加载策略仍待排查。[失败日志](../assets/hd-ai/2026-09-08/replay-hd-map.log)、[RT64 加载方式](https://github.com/rt64/rt64/blob/main/TEXTURE-PACKS.md#operation)

## 看图后的下一阶段方向

- **头像**：以 Qwen 3.0 候选 2 为当前首选。后续先验证源 Alpha 合成、游戏实际显示尺寸和原角色特征，再用少量其他人物检查风格与身份稳定性。冻结已选输出，不把重新调用模型当作相同候选。
- **文字**：继续采用轮廓字体。
- **UI 边框**：AI 处理效果不显著，暂缓生成；如需继续，单独验证按尺寸重建的边框／九宫格。
- **机械单位图标**：不处理，沿用原素材。
- **森林等地图 tile**：暂缓，当前不投入批量生成与替换。
- **过场地图**：保留观察；后续若继续，先说明实际显示下的可见收益，再评估扩大范围。已有回放只证明该快照内的替换链路，不代替实时游戏流程与跨平台验证。

欧洲图原始候选的低分辨率水域蒙版 IoU 为 0.9779–0.9922；它反映本样本的粗略轮廓漂移，不能证明岛屿身份、山脉位置或视觉质量全部正确。未据此计算“生产通过率”。所有候选仍是试验材料，尚未发布。

## 复现与文件边界

脚本位于 `tools/hd_ai/`。生成图片、ROM 解码内容、调色板、捕获内存、临时依赖和报告全部位于被忽略的 `assets/hd-ai/`；原 ROM、实时游戏源码和现有字体包未修改。未执行 commit／push。

```sh
.venv/bin/python tools/hd_ai/extract_samples.py
.venv/bin/python -m tools.hd_ai.prepare_samples
# 有费用的运行必须沿用冻结清单。已存在请求记录会跳过，不会自动重试 POST。
.venv/bin/python -m tools.hd_ai.run_approved_batch /absolute/path/to/existing/.env
.venv/bin/python -m tools.hd_ai.review_outputs
.venv/bin/python -m tools.hd_ai.build_map_probe
```

`prepare_samples` 内保存了本轮提示词，并要求已有真实快照／纹理转储；不是脱离本工程证据的通用资源导出器。欧洲模型输入使用原调色板的 RGB 作为不透明底图，运行时包另外恢复原调色板 Alpha。已生成候选不应在生产构建时再次请求模型。

## 本地交互浏览页

从已保存的结果重新生成页面，不调用模型：

```sh
.venv/bin/python tools/hd_ai/build_gallery.py
.venv/bin/python -m http.server 8769 --bind 127.0.0.1 --directory assets/hd-ai/2026-09-08/gallery
```

浏览器打开 <http://127.0.0.1:8769/>。也可直接打开 `gallery/index.html`；需保留旁边的 `media/` 目录。页面支持六类素材、四个模型与独立候选切换，原始输出／恢复透明蒙版、最近邻／Lanczos 基线、滑杆／并排、放大、原尺寸下载，以及捕获帧地图／地图＋字体对照。网页仅从冻结结果复制图片并生成浏览缩略图；`gallery-manifest.json` 记录本地文件 SHA-256。服务目录仅包含网页及图片，不包含 ROM、捕获内存或 API 请求记录。
